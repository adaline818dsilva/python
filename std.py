import numpy as np
a = np.array([3, 3, 3])
b = np.array([4, 5, 6])
print(a + b)
print(a * b)

# Continue with more numpy operations
print(a - b)         # Subtraction
print(a / b)         # Element-wise division
print(np.dot(a, b))  # Dot product
